extends layout/main
include mixins/videoBlock

block content
    .search__header
        h4 Searching for #{searchingBy}
    .videos
        each item in videos
            +videoBlock({
              id: item.id,
              title: item.title,
              views: item.views,
              videoFile: item.videoFile,
            })